server:
  port: 8761

spring:
  application:
    name: Micro_mc_discovery-server
  config:
    activate:
      onProfile: discovery-server-1

eureka:
  client:
    fetchRegistry: false
    registerWithEureka: false
  datacenter: PeruAmerica1
  environment: Develoment
